# 🏗️ Step 4 – Build and Run Your Image

Build your custom image:

```bash
docker build -t myapp .
```

Run it:

```bash
docker run --rm myapp
```

👉 You should see:

```
Hello from my Docker container!
```
